import { Box } from "@material-ui/core";
import React from "react";
import "./style.css";

interface TopLayoutProps {}

const TopLayout: React.FC<TopLayoutProps> = ({}) => {
  return <Box className="top-layout"></Box>;
};

export default TopLayout;
